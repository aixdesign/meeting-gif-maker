#!/usr/bin/env bash
# Double-click me to start the Meeting GIF Maker.
# A Terminal window will open (that's the engine running) and your browser
# will open the tool automatically. Close the Terminal window to stop it.
cd "$(dirname "$0")"
exec ./run.sh
